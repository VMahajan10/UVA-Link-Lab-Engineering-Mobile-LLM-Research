<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- External files directory for CSV exports -->
    <external-files-path 
        name="external_files" 
        path="." />
    
    <!-- Documents directory for CSV files -->
    <external-files-path 
        name="documents" 
        path="Documents/" />
    
    <!-- Models directory for LLM model files -->
    <external-files-path 
        name="models" 
        path="models/" />
    
    <!-- Cache directory for temporary files -->
    <external-cache-path 
        name="external_cache" 
        path="." />
    
    <!-- Internal files directory -->
    <files-path 
        name="internal_files" 
        path="." />
    
    <!-- Internal cache directory -->
    <cache-path 
        name="internal_cache" 
        path="." />
</paths>
